# SWAT+ 镇西水文站模拟诊断报告

## 运行状态

- **模型版本**: SWAT+ v61.0.2 (Intel ifx static build)
- **模拟时段**: 2012-01-01 至 2022-12-31 (11年)
- ** outlet 河道**: `cha0302` (gis_id=303, unit=302)
- **输出文件**: `channel_day.txt` (1,217,457 行, 1.15 GB) — 完整11年数据

## 模拟结果统计

| 指标 | 验证期 (2015-2017) | 率定期 (2018-2022) | 全时段 (2012-2022) |
|------|-------------------|-------------------|-------------------|
| NSE  | -4.287            | -2.804            | -2.448            |
| KGE  | -0.520            | -0.216            | -0.143            |
| PBIAS| +1.3%             | -1.9%             | -22.9%            |
| R²   | 0.050             | 0.082             | 0.073             |
| RMSE | 89.01             | 116.10            | 101.99            |

- **模拟均值流量**: 31.3 m³/s
- **观测均值流量**: 40.6 m³/s
- **模拟最大流量**: 1311 m³/s (高估)
- **观测最大流量**: 460 m³/s

## 关键诊断发现

### 1. 基流完全缺失 (latq = 0)

**根本原因**: `soils.sol` 中所有土壤仅有一层 (`NLY = 1`)。

SWAT+ 源码 `swr_percmicro.f90` 第 39-43 行:
```fortran
if (ly1 == 1) then
  latlyr = 0.
else
  latlyr = hru(j)%hyd%latq_co * ho * soil(j)%phys(ly1)%k * hru(j)%topo%slope / hru(j)%topo%lat_len * .024
end if
```

当 `NLY = 1` 时，`ly1` 永远为 1，侧向流 `latlyr` 被强制置零。

**尝试修复**: 将 `NLY` 改为 2（第一层 300mm + 第二层 700mm），`latq` 仍然接近 0 mm/年。

**原因**: 即使 `NLY = 2`，土壤平均含水量 (`sw_ave = 84 mm`) 仍远低于田间持水量 (`fc ≈ 180 mm`)，`sw_excess = st - fc` 始终为负，因此 `latq` 无法产生。

**结论**: 在半干旱气候（年降水 413 mm，PET 705 mm）下，侧向流 (`latq`) 本身就应该很小。基流应主要来自地下水回流 (`gwq`)。

### 2. 地下水模块未启用

当前 `hru.con` 中所有 HRU 的 `obtyp = cha`，产流直接入河，未经过 `aquifer` 或 `gwflow` 模块。

- `file.cio` 中 `aquifer` 数据库为 `null`
- `aquifer.aqu` 中所有属性为 `null`
- `command.f90` 中存在 `case("gwflow")` 和 `case("aqu")`，但 `ob` 数组中无此类对象

**影响**: 深层入渗 (`perc`) 的水量（约 2.6 mm/年）永久丢失，无法以基流形式返回河道。

### 3. 土壤深度敏感性测试

| 土壤深度 | NSE | PBIAS | 模拟均值 | 关键问题 |
|---------|-----|-------|---------|---------|
| 50 mm   | -2.45 | -22.9% | 31.3 m³/s | 基流缺失，峰值过高 |
| 200 mm  | -0.79 | -59.2% | ~17 m³/s | 基流缺失，总量偏低 |
| 1000 mm | -0.33 | -81.2% | 7.6 m³/s | 基流缺失，总量严重偏低 |

土壤深度增加导致更多水分被土壤储存/蒸散发，径流总量下降。

### 4. 峰值过高

模拟峰值流量（1200+ m³/s）远超观测峰值（460 m³/s）。

可能原因:
- `CN` 值设置导致地表径流 (`surq`) 过于集中
- 河道演算 (`msk_co1=0.75, msk_co2=0.25`) 对洪峰平滑不足
- `surq_lag = 4` 天延迟过短

### 5. 流域面积覆盖疑问

- `channel.con` 中 `cha0302` 面积 = 18,442,270,720 m² = **18,442 km²**（与实际镇西站集水面积 18,462 km² 几乎一致）
- `hru.con` 中 1652 个 HRU 总面积 = 783,181（单位疑似 ha）= **7,832 km²**
- HRU 覆盖面积仅为实际汇水面积的 **~42%**

如果 58% 的流域面积未被 HRU 覆盖，模拟径流自然会偏低。

## 下一步建议

### 高优先级

1. **启用地下水模块**
   - 在 `hru.con` 中将 HRU 输出连接到 `aquifer` 对象（而非直接到 `cha`）
   - 配置 `aquifer.aqu` 参数（如 `gw_flo`, `gw_delay`, `alpha_bf`）
   - 将 `aquifer` 连接到河道，使 `perc` 水量能以基流形式返回

2. **检查/修正流域面积覆盖**
   - 验证 `hru.con` 中 `area` 的单位（ha vs m²）
   - 检查 DEM/流域提取过程，确认 HRU 是否覆盖整个 18,442 km²
   - 若 HRU 面积确实只有 7,832 km²，需要重新划分 HRU

3. **平滑模拟峰值**
   - 增加 `surq_lag`（如从 4 改为 10-15 天）
   - 调整 `msk_co1`/`msk_co2`（如 2.0 / 1.0）以增加河道演算平滑
   - 或调整 `cn3_swf` 以改变 CN 曲线行为

### 中优先级

4. **增加土壤层数**
   - 将 `soils.sol` 从 `NLY=1` 改为 `NLY=2`（需确保格式正确）
   - 第二层深度应大于第一层（如 300mm + 1000mm）
   - 预期效果有限（气候干燥，`latq` 仍可能接近 0）

5. **参数率定**
   - 使用 `calibrate_params.py` 对 `CN2`, `ESCO`, `EPCO`, `SOL_AWC`, `SOL_K` 等进行系统率定
   - 目标：改善 NSE > 0.5，PBIAS < ±15%
